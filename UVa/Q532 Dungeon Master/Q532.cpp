#include<iostream>
#include<vector>
#include<queue>
#define f first
#define s second
#define AC ios::sync_with_stdi(0), cin.tie(0);
using namespace std;

int const N = 1e4;
int result[N][N], t, n, m, sx, sy, ex, ey;
string s;
vector<vector<string>> vec, tmp;

signed main() {
    while(cin >> t >> n >> m) {
        for(int i=0; i<)
    }
    return 0;
}
